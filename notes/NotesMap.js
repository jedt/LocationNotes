import {StyleSheet, View, Text} from 'react-native';

export default function NotesMap({navigation}) {
  return (
    <View style={styles.container}>
      <Text>Empty</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    padding: 10,
  },
});
